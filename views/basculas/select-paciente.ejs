<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        .card {
            transition: transform 0.3s ease;
            border: 1px solid #dee2e6;
        }
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }
        .card-icon {
            font-size: 2.5rem;
            color: #007bff;
            margin-bottom: 15px;
        }
        .badge.bg-pink {
            background-color: #e83e8c;
            color: white;
        }
        .btn-primary {
            padding: 8px 20px;
        }
    </style>
</head>
<body>
    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>
                <i class="fas fa-user-plus me-2"></i><%= title %>
            </h1>
            <a href="/basculas" class="btn btn-outline-secondary">
                <i class="fas fa-arrow-left me-2"></i>Volver a Registros
            </a>
        </div>
        
        <p class="lead mb-4">Seleccione un paciente para registrar su peso</p>
        
        <div class="row">
            <% if (pacientes && pacientes.length > 0) { %>
                <% pacientes.forEach(paciente => { %>
                    <div class="col-md-4 mb-4">
                        <div class="card h-100">
                            <div class="card-body text-center">
                                <i class="fas fa-user-circle card-icon"></i>
                                <h5 class="card-title"><%= paciente.nombre %> <%= paciente.apellido %></h5>
                                
                                <% if (paciente.edadCalculada) { %>
                                    <p class="card-text">
                                        <strong>Edad:</strong> <%= paciente.edadCalculada %> años
                                    </p>
                                <% } else if (paciente.fecha_nacimiento && calcularEdad) { %>
                                    <p class="card-text">
                                        <strong>Edad:</strong> <%= calcularEdad(paciente.fecha_nacimiento) %> años
                                    </p>
                                <% } %>
                                
                                <% if (paciente.sexo) { %>
                                    <p class="card-text">
                                        <strong>Sexo:</strong> 
                                        <% if (paciente.sexo === 'M') { %>
                                            <span class="badge bg-info">Masculino</span>
                                        <% } else { %>
                                            <span class="badge bg-pink">Femenino</span>
                                        <% } %>
                                    </p>
                                <% } %>
                                
                                <p class="card-text text-muted">
                                    <small>ID: <%= paciente.id %></small>
                                </p>
                                
                                <a href="/basculas/nuevo/<%= paciente.id %>" class="btn btn-primary mt-2">
                                    <i class="fas fa-plus me-1"></i>Registrar Peso
                                </a>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="col-12">
                    <div class="alert alert-warning text-center py-5">
                        <i class="fas fa-users-slash fa-3x mb-3 text-warning"></i>
                        <h4>No hay pacientes registrados</h4>
                        <p class="mb-4">Debe crear pacientes primero para poder registrar pesos.</p>
                        <a href="/pacientes/nuevo" class="btn btn-warning">
                            <i class="fas fa-user-plus me-2"></i>Crear Nuevo Paciente
                        </a>
                        <a href="/basculas" class="btn btn-outline-secondary ms-2">
                            <i class="fas fa-arrow-left me-2"></i>Volver
                        </a>
                    </div>
                </div>
            <% } %>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>