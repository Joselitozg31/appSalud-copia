<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Temperaturas' %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-hospital me-2"></i>AppSalud
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">
                            <i class="fas fa-home me-1"></i>Inicio
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/pacientes">
                            <i class="fas fa-users me-1"></i>Pacientes
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/basculas">
                            <i class="fas fa-weight me-1"></i>Básculas
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/temperaturas">
                            <i class="fas fa-thermometer-half me-1"></i>Temperaturas
                        </a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            <i class="fas fa-user me-1"></i><%= usuario ? usuario.username : 'Usuario' %>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><a class="dropdown-item" href="/logout"><i class="fas fa-sign-out-alt me-2"></i>Cerrar Sesión</a></li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <main class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <% if (paciente) { %>
                    <h1 class="h3"><i class="fas fa-user me-2"></i><%= paciente.nombre %> <%= paciente.apellido %></h1>
                    <small class="text-muted">
                        Registros de temperatura del paciente
                        <% if (paciente.fecha_nacimiento) { %>
                            · Edad: <%= calcularEdad(paciente.fecha_nacimiento) %> años
                        <% } %>
                    </small>
                <% } else { %>
                    <h1 class="h3"><i class="fas fa-thermometer-half me-2"></i>Registros de Temperatura</h1>
                <% } %>
            </div>
            <div>
                <a href="/" class="btn btn-outline-secondary me-2"><i class="fas fa-arrow-left me-1"></i>Volver al Inicio</a>
                <% if (paciente) { %>
                    <a href="/temperaturas/nuevo/<%= paciente.id %>" class="btn btn-primary"><i class="fas fa-plus me-1"></i>Nuevo Registro</a>
                <% } else { %>
                    <a href="/temperaturas/nuevo" class="btn btn-primary"><i class="fas fa-plus me-1"></i>Nuevo Registro</a>
                <% } %>
            </div>
        </div>

        <div class="card">
            <div class="card-body">
                <% if (registros && registros.length > 0) { %>
                    <div class="table-responsive">
                        <table class="table table-hover" id="temperaturasTable">
                            <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>Paciente</th>
                                    <th>Fecha</th>
                                    <th>Temperatura (°C)</th>
                                    <th>Temperatura (°F)</th>
                                    <th>Acciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% registros.forEach(r => { %>
                                    <tr>
                                        <td><%= r.id %></td>
                                        <td>
                                            <% if (r.nombre && r.apellido) { %>
                                                <%= r.nombre %> <%= r.apellido %>
                                            <% } else if (paciente) { %>
                                                <%= paciente.nombre %> <%= paciente.apellido %>
                                            <% } else { %>
                                                Paciente N.º <%= r.paciente_id %>
                                            <% } %>
                                        </td>
                                        <td><%= r.fecha_medicion ? new Date(r.fecha_medicion).toLocaleString('es-ES') : (r.fecha_registro ? new Date(r.fecha_registro).toLocaleString('es-ES') : '-') %></td>
                                        <td><%= r.temperatura %></td>
                                        <td><%= (r.temperatura * 9/5 + 32).toFixed(1) %></td>
                                        <td>
                                            <div class="btn-group" role="group">
                                                <a href="/temperaturas/editar/<%= r.id %>" class="btn btn-sm btn-outline-primary"><i class="fas fa-edit"></i></a>
                                                <button class="btn btn-sm btn-outline-danger delete-temp" data-id="<%= r.id %>"><i class="fas fa-trash"></i></button>
                                            </div>
                                        </td>
                                    </tr>
                                <% }) %>
                            </tbody>
                        </table>
                    </div>

                    <div class="mt-3 d-flex justify-content-between">
                        <div class="text-muted">Mostrando <%= registros.length %> registros</div>
                        <div>
                            <button id="exportCsv" class="btn btn-outline-success btn-sm"><i class="fas fa-file-csv me-1"></i>Exportar CSV</button>
                        </div>
                    </div>
                <% } else { %>
                    <div class="text-center py-5">
                        <i class="fas fa-thermometer-half fa-4x text-muted mb-3"></i>
                        <h4>No hay registros de temperatura</h4>
                        <p class="text-muted">Agrega una nueva medición para comenzar.</p>
                        <a href="/temperaturas/nuevo" class="btn btn-primary mt-3">Registrar Temperatura</a>
                    </div>
                <% } %>
            </div>
        </div>
    </main>
    
<footer class="bg-light py-4 mt-5 border-top">
        <div class="container text-center">
            <p class="mb-2 text-muted">
                <i class="fas fa-heartbeat text-danger me-1"></i>
                AppSalud - Sistema de Gestión Médica
            </p>
            <small class="text-muted">© 2024 - Versión 1.0.0</small>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/temperaturas.js"></script>
</body>
</html>
